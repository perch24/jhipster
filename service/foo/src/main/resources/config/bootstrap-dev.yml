# ===================================================================
# Spring Cloud Consul Config bootstrap configuration for the "dev" profile
# ===================================================================

spring:
    cloud:
        discovery:
          enabled: true
        consul:
            config:
                enabled: true
                fail-fast: true
                format: yaml
                profile-separator: "-"
